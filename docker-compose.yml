services:
  # Remote mode (for HTTP/SSE connections) - Default
  notion-mcp-server:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Stdio mode (for local MCP connections)
  notion-mcp-server-stdio:
    build: .
    stdin_open: true
    tty: true
    restart: unless-stopped
    command: ["node", "bin/cli.mjs"]
    environment:
      - NOTION_API_KEY=${NOTION_API_KEY}
      - NOTION_API_VERSION=${NOTION_API_VERSION:-2022-06-28}
      - BASE_URL=${BASE_URL:-https://api.notion.com}
